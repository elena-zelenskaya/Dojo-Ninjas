Run the shell and import your models 
from dojo_ninjas_app.models import Dojo, Ninja

Query: Create 3 new dojos
Dojo.objects.create(name="T", city="Los Angeles", state="CA")
Dojo.objects.create(name="M", city="New York", state="NY")
Dojo.objects.create(name="P", city="San Francisco", state="CA")

Query: Delete the 3 dojos you just created
for dojos in Dojo.objects.all():
    dojos.delete()

Query: Create 3 more dojos
Dojo.objects.create(name="W", city="San Francisco", state="CA")
Dojo.objects.create(name="Y", city="Los Angeles", state="CA")
Dojo.objects.create(name="Z", city="New York", state="NY")

Query: Create 3 ninjas that belong to the first dojo
Ninja.objects.create(first_name="Tom", last_name="S", dojo_id = Dojo.objects.first())
Ninja.objects.create(first_name="Jack", last_name="R", dojo_id = Dojo.objects.first())
Ninja.objects.create(first_name="Tyrone", last_name="N", dojo_id = Dojo.objects.first())

Query: Create 3 ninjas that belong to the second dojo
Ninja.objects.create(first_name="Pete", last_name="A", dojo_id=Dojo.objects.get(name="Y"))
Ninja.objects.create(first_name="Tim", last_name="B", dojo_id=Dojo.objects.get(name="Y"))
Ninja.objects.create(first_name="Andrew", last_name="C", dojo_id=Dojo.objects.get(name="Y"))

Query: Create 3 ninjas that belong to the third dojo
Ninja.objects.create(first_name="Dave", last_name="G", dojo_id=Dojo.objects.last())
Ninja.objects.create(first_name="Paul", last_name="O", dojo_id=Dojo.objects.last())
Ninja.objects.create(first_name="Mark", last_name="J", dojo_id=Dojo.objects.last())

Query: Retrieve all the ninjas from the first dojo
Dojo.objects.first().ninjas.all()

Query: Retrieve all the ninjas from the last dojo
Dojo.objects.last().ninjas.all()

Query: Retrieve the last ninja's dojo
Ninja.objects.last().dojo_id

Query: Create a new dojo
Dojo.objects.create(name="New!", city="Castro Valley", state="CA", desc="THE NEW ONE!")